{
  "name": "cb01-router",
  "version": "1.0.0",
  "private": true,
  "description": "CB01 Router — classifies and dispatches requests to NOIZY agents",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241004.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.80.0"
  }
}
